# Runtime dependencies for the AWS Lambda image — narrower than the dev
# requirements.txt to keep the container small.
#
# Versions are bounded above to avoid pulling experimental releases that
# don't ship pre-built wheels for our (linux/arm64, Python 3.11) combo
# and would force a from-source build inside the slim Lambda base image.

# LLM client
groq>=0.13.0,<2

# Data engineering runtime — pinned to majors with stable wheel coverage.
pandas>=2.2.0,<3
pyarrow>=15.0.0,<20
# numpy capped below 2.3 — newer releases have intermittent wheel coverage
# for Python 3.11 on linux-arm64.
numpy>=1.26,<2.3

# Validation
pydantic>=2.7.0,<3

# AWS SDK is preinstalled in Lambda runtime images, but pin it for clarity
# during local docker build.
boto3>=1.34.0,<2
