"""DataPolish v2 — multi-page Streamlit UI.

Entry point: `ui/Home.py`. Run with:
    streamlit run ui/Home.py

The UI never reads files from disk directly. Every interaction goes
through the FastAPI in `api/main.py` (default http://localhost:8000).
"""
